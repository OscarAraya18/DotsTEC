package Asignation.Class;

import org.json.simple.JSONArray;
import Code.Class.*;;

public class Asignation {
	@SuppressWarnings("unchecked")
	public JSONArray arrayUsers() {
		JSONArray Array = new JSONArray();
		Array.add(new Code(98766855));
		return Array;
	}
}
